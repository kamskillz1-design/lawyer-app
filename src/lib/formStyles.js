// Shared form-control styling used across staff pages and dialogs.
// Keep every form visually consistent by importing these instead of
// re-declaring the class string per file.
export const inputClass = "w-full h-9 rounded-md border border-input bg-card px-3 text-sm";
export const labelClass = "grid gap-1 text-sm text-muted-foreground";